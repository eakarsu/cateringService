import {useAuth} from '../context/AuthContext';import{ShoppingCart,LogOut,ShieldCheck}from'lucide-react';
export default function Layout({children}){const{user,logout}=useAuth();async function signOut(){await logout();location.assign('/login');}return <div className="min-h-screen bg-slate-50"><aside className="fixed inset-y-0 left-0 w-64 bg-slate-950 text-white p-5"><h1 className="text-xl font-bold">Catering Pro</h1><p className="text-xs text-slate-400 mt-1">Governed commerce</p><div className="sidebar-link active mt-8"><ShoppingCart size={19}/>Order operations</div><div className="absolute bottom-5 left-5 right-5 border-t border-slate-700 pt-4"><p className="font-medium truncate">{user?.name}</p><p className="text-xs text-slate-400 flex gap-1 items-center"><ShieldCheck size={13}/>{user?.role} · tenant scoped</p><button onClick={signOut} className="mt-4 flex gap-2 text-slate-300"><LogOut size={18}/>Sign out</button></div></aside><main className="ml-64 p-8">{children}</main></div>}
